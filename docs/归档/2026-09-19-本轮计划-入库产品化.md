# 本轮计划：3.3 入库产品化

规格：`docs/本轮Spec.md`。先闸门后实现。TDD。

## 任务

1. **书入库薄地图闸门** — `index.map_thin` + 骨架写 `## 别名` + 入库回报必出工作单 + scan 标骨架未加厚。测试先红后绿。
2. **笔记冲突段 + 短地图 + invalidate** — preview 列候选；create/update 覆盖同名标题段；无标题同题不新开；写盘 invalidate。测试先红后绿。
3. **配套** — 阅读日志基身份（`#n` 算读过该章）；lint 新建地图测试；server/README 文案。
4. **Skill** — 仓库登记 + Junction；`skills/knowledge/SKILL.md` 与 `docs/Skill-资料室.md` 对齐。
5. **验收** — pytest 全绿；真库 Q1–Q14 + W1–W16；准/全/省只许升或平；验收记录 + 打分记录。

## 不做

第七扇门、入库调模型、改书正文、改冻结题集、清 baoyu-* 历史债。
