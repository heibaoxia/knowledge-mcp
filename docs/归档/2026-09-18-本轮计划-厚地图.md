# 本轮计划：3.1 厚地图

> 规格：`docs/本轮Spec.md`。存档点：`579ab3a`（3.0.0）。

## 做

1. 人审 Spec。
2. 七本 `fill_chapter_names`（机械，不调模型）。
3. Agent 写别名 + 必要时改能解决什么；代码 `map_problems` / scan 校验。
4. pytest 全绿；真库 Q/N/W 打准/全/省；两处记录。
5. 12d：证据片极大化 + 别名只整串包含 + 无长片时用最稀二字片（见 Spec）。

## 不做

再入库、改书正文、改冻结题集、在 retrieve 里写史书专名、换嵌入。

## 做完

归档本 Spec + 本计划；`docs/版本.md` 与 `pyproject` 去掉 `-dev`。
