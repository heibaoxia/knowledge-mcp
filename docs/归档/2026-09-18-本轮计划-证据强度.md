# 本轮计划：3.2 证据强度

> 规格：`docs/本轮Spec.md`。基线：tag `v3.1.0`。

## 做

1. T15 二字别名前缀扩写；T16 汉词缺席拉丁不得顶。
2. `_expand_ok` / `_longest_existing_prefix` / `_match_gate`。
3. pytest 全绿；真库全组；两处记录。

## 不做

重构检索层；挤掉相关第二本；改冻结准口径；改书正文。

## 做完

归档 Spec + 本计划；版本去 `-dev`。
