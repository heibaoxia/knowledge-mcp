# 本轮计划：第三版检索

> 规格：`docs/本轮Spec.md`。上级：`需求.md`。架构：`docs/重构架构.md`。

## 段 A（先做，无模型）

1. 归档确认第 9 轮 Spec 已在 `docs/归档/2026-09-16-本轮Spec-认册卷切块.md`。
2. 地图：`parse_map` / `map_problems` 加章名、别名；入库机械补章名段。
3. FTS 列：title/name/aliases/body + bm25 权重。
4. `CANDIDATE_LIMIT=30`，输出仍 5；栏名仅语义；建议块篇名优先。
5. 别名扩写（只专名）。
6. 红测 T1–T9、T11–T12 先红后绿；旧 S/V 全绿。
7. gitignore `mcp/.kb-index.sqlite`。

## 段 B（接向量）

1. 可注入嵌入接口；默认假嵌入跑 pytest。
2. 一块一向量，输入书名+块名+指向该块的地图句，正文最多 2000 字。
3. RRF 融合；`KNOWLEDGE_EMBED=off` 不崩。
4. 真库全组打分；流水写机器号 `3.0.0-dev` 或去掉 `-dev` 的条件见 Spec §8–§9。

## 不做

改书、OCR、jieba、灌摘录、改冻结题集、为 N8 写「林彪」特判。

## 做完

pytest 全绿 + 真库 Q/N/W + 两处打分记录。然后归档 Spec+计划+Spike，`docs/本轮Spec.md` 拿掉，`docs/版本.md` 去掉 `-dev`。
