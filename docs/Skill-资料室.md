# Skill：知识库（资料室）

机器 id：`knowledge`（文件夹 `skills/knowledge/`）。对人仍叫知识库。

> 给人点名启用。闸门在 MCP，不在本文。
> 上级：`需求.md`。正文与规范根 `skills/knowledge/SKILL.md` 同一实质。

## 何时用

用户说「用知识库」「用资料室」「查库里的书」或点名本 Skill。没点名，不要去调 `kb_*`。

## 开门

这场对话必须已经接上本机 `knowledge` MCP。若工具列表里没有 `kb_search`：告诉用户去启用该 MCP（命令指向仓库 venv 的 `knowledge-mcp`，工作目录 `F:\project\knowledge`），不要假装查过。

## 用法（短）

1. **先 `kb_search`**，只拿路标（身份、建议块、三栏）。路标里没有正文。
2. **再 `kb_read` 点名** `书/<slug>/<章>` 或 `笔记/<slug>`。一次最多 5 块，单窗 8000，整次 24000。超长用 `章名#2`。
3. 够答就停。不要按目录通读，不要打开 `资料/` 灌全文。

## 入库：写完骨架不算入完

丢书走 `kb_ingest_inbox` / `kb_ingest_files`（点名源文件路径，不是书名）。入库只写**六段骨架地图**，薄骨架不算入完；回报会附「编地图」工作单。按工作单加厚才算入完：

- 「能解决什么」改成**读者问法**（3–8 条，每条 ≤40 字），不是章名
- 「别名」至少 3 条：`<专名 ≥2 字> → 书/<slug>/<已存在的块名>`，不许拿公共词表凑
- 「不解决什么」加 1–4 条库里**其它书**才有的问法
- 走 `kb_lint_notes(action=apply)`，plan 里 `{"op":"update","target":"书/<slug>/地图","markdown":"<整份地图>"}`；地图不存在也能这样新建
- 别名与问法由 Agent 编——工具只给清单和校验，不代写

加厚后 `kb_lint_notes scan` 不再报该书「骨架未加厚」，才算入完。

## 写笔记：先看冲突段

`create` / `update` 前先 `preview` → `verify`（相符 / 部分不符 / 库中无 / 非事实）。说「相符」必须先成功 `kb_read` 过依据。

- `preview` 会列**冲突段候选**：其它笔记里与本稿同名的二级/三级标题。有这一栏就先看，别硬写。
- `create` 写入后，其它笔记里**同名标题段**的正文会被换成新稿的，节首标 `> 已被 笔记/<新slug> 覆盖`。YAML 标题相同且双方都没有二/三级标题：不新开，改为更新旧条。只是检索相似、标题不同：不改旧条。
- 写入会补短地图并失效索引。

## 退书

`kb_lint_notes` 先 `scan`，再 `apply` `withdraw`，点名 `书/<slug>`。不要删文件夹、不要删某一章。

## 不要做

- 把检索结果里的正文摘录进回答（工具也不会给）
- 库里没有却装查过
- 没点名就启用资料室工具
- 入库写完骨架就报「入完」，不走工作单加厚
- 绕开 `preview` 的冲突段栏直接写笔记
