# 下一轮开工（贴进新对话）

把下面「提示词」整段复制到新对话。本文件只给人用，不是规格。

---

## 提示词

你是本机资料室 MCP 的实现 Agent。工作目录 `F:\project\knowledge`。分支必须是 `feat/v3-hybrid-retrieve`（先 `git status` / `git branch` 核对）。产品代 `3.0.0-dev`。

### 先读（按序，不要凭记忆）

1. `AGENTS.md`
2. `需求.md`（第三版合同）
3. `docs/本轮Spec.md`（实现规格，改 `mcp/` 只准按它）
4. `docs/本轮计划.md`（先段 A 后段 B）
5. `docs/重构架构.md`、`docs/借鉴与开源.md`
6. `docs/版本.md`、`docs/Skill-资料室.md`（Skill 是开门，本轮 **不要** 改客户端挂载）

### 你要做的

按 `docs/本轮计划.md` **自己写实现步骤再改代码**。本对话不写代码，规格已收口。

- **先段 A**（无嵌入模型）：地图章名/别名、FTS 分列加权、候选池 30、栏名「仅语义」、建议块篇名优先、别名扩写、gitignore 已有 `mcp/.kb-index.sqlite`。合成闸 T1–T9、T11–T12 先红后绿。
- **再段 B**：章向量 + RRF；pytest 用假嵌入；`KNOWLEDGE_EMBED=off` 不崩。
- 做完：pytest 全绿；真库 Q1–Q14 + N 组 + W1–W16；按 `docs/验收标准.md` 第九节打准/全/省；追加 `检修/验收记录.md` 和 `检修/打分记录.md`。没有这两节不许说做完、不许归档 Spec。

### 红线

- 不改书正文；检索路标无正文；不上 jieba；不灌 RAG 摘录；不为单题/单书写过滤器。
- 不改冻结题集期望；不改「准」二值口径（Spec 已写明）。
- 禁止出现黄金海/文革史/林彪/武斗/Rust/埃里克森/实践论等真名当代码规则。
- 旧测试 S1–S12、V1–V4、路标无正文、PyTorch 空必须绿。
- 六扇门不增不拆。Skill 开门不在本轮 `mcp/` 里实现。

### 工作区

未提交改动可能很多（第 7–9 轮代码 + 第三版文档）。先看 `git status`，不要 `reset` 丢掉别人的文档。实现 diff 尽量只动 Spec 点名的文件。

从段 A 的红测开始。
