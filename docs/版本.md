# 版本

> 人话记在这里。机器号在 `mcp/pyproject.toml` 的 `version`。两者必须一致。

## 怎么编号

**大.小.补**（SemVer），和「第几版资料室」对齐：

| 位 | 什么时候加 | 例子 |
|---|---|---|
| **大** | 产品合同或检索引擎换代（人说的「第 N 版」） | `2.x` → `3.0.0` |
| **小** | 同一代里做成一块能力（地图加厚、字段加权、章向量……） | `3.0.0` → `3.1.0` |
| **补** | 修 bug、改文档、闸门微调，合同不变 | `3.0.0` → `3.0.1` |

预发布：代码还没落地时，可以在号后面加 `-dev`（如 `3.0.0-dev`）。一做完、打过分，去掉 `-dev`。

**不要**用「第 3.1 版」这种口头号当 git tag。tag 写成 `v3.0.0`。

分支：3.1 收口在 `feat/v3-hybrid-retrieve`，tag `v3.1.0`。下一刀（3.2）在 `feat/v3.2-precision`，不在 3.1 分支上继续改检索。

## 现在

| 项 | 值 |
|---|---|
| 人话 | **第三版** · 入库默认骨架地图 |
| 机器号 | `3.2.2` |
| 分支 | `feat/v3.2-precision` |
| 规格 | `需求.md` · `docs/归档/2026-09-18-本轮Spec-入库骨架地图.md` |

## 对照（给人看，不是 git 史）

| 大版本 | 人话 | 检索长什么样 |
|---|---|---|
| **1** | 立项、空 MCP | 六扇门架子 |
| **2** | 自然篇 + 语片 FTS + 地图两路 + 闸门堆准度 | 只认字 + 薄地图；`pyproject` 当时仍是 `0.0.1`，没跟代走 |
| **3** | 资料室合同留下，检索引擎推倒 | 字段加权 FTS ∥ 章向量，RRF 融合，路标仍无正文 |

第 2 版里的第 3～9 轮（闲书闸、最长片、aboutness、退书、认册卷）都算 **2.x 的小步**，不再单独占大版本。

## 改号时做什么

1. 改 `mcp/pyproject.toml` 的 `version`
2. 在本文件「现在」表改一格，文末追加一行流水
3. 大版本：更新 `需求.md` 修订表；小/补：打分记录里写机器号即可
4. 落地打过分之后：`git tag v大.小.补`

## 流水

| 日期 | 号 | 说明 |
|---|---|---|
| 2026-09-17 | 3.0.0-dev | 开第三版分支；规格收口，代码未落地 |
| 2026-09-17 | 3.0.0 | 字段 FTS + 章向量落地；第 10 轮假嵌入、第 11 轮 BGE-M3 三维同分 准 23/29、全 31/32、省 26/26。git `579ab3a` |
| 2026-09-17 | 3.1.0-dev | **准备**：七本真库地图加厚（章名机械补 + Agent 写别名）。不入库、不改检索主路径 |
| 2026-09-18 | 3.1.0 | 七本厚地图 + 证据片通则（整串包含扩写、极大原片、套话动词不当证据）。第 12 轮收口 准 24/29、全 32/32、省 26/26、W 16/16 |
| 2026-09-18 | 3.2.0-dev | 从 `v3.1.0` 开 `feat/v3.2-precision`。Spec：二字别名前缀扩写 + 汉词缺席不让拉丁顶。同主题多本不挤（D5） |
| 2026-09-18 | 3.2.0 | 二字别名前缀扩写；≥4 字汉词 DF=0 不收到 2 字；汉词缺席则拉丁不得顶。第 13 轮 准 26/29、全 32/32、省 26/26 |
| 2026-09-18 | 3.2.1 | **准口径**：闲书=证据对不上章名/别名/地图，不再「非期望即 0」。检索未改。第 14 轮对照准 29/29，全/省/W 与 13 轮相同 |
| 2026-09-18 | 3.2.2 | 入库机械写骨架地图（章名+能解决什么取自篇名；别名空）。俗称仍 Agent 加厚。第 16 轮 准 29/29 全 32/32 省 26/26 W 16/16 |

## 3.0 已经改了什么（存档点）

- 检索引擎：字段加权 FTS ∥ 章向量 + RRF；栏名仅语义；别名扩写；建议块篇名优先。
- 嵌入：pytest 假向量；真库硅基流动 `BAAI/bge-m3`（Key 不进 git）。
- 地图**校验器**已加章名覆盖/别名规则；**真库七本地图仍薄**（章名 0、别名 0）。
- 打分：第 10–11 轮 准 23/29、全 31/32、省 26/26。

## 3.1 已经改了什么

- 真库七本地图加厚：章名机械补齐 + Agent 别名 461 条（正本 gitignore）。
- 证据片：极大原片；别名扩写只认整串包含；无长片时最稀二字；`形成` 与 `发生/变化` 同类不当证据。
- aboutness 认 `map_index_text`（含别名）。空证据不得把仅字面抬成两路都中。
- 打分：第 12 轮收口 准 24/29、全 32/32、省 26/26、W 16/16。对照第 11 轮准升、全升、省平。
