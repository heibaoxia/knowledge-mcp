# 参考笔记 · 开源对照

> **每次和用户讨论本项目之前先读这一份**（至少读 §0–§2，再按话题跳到映射节）。
> 现行规格以 `需求.md` 为准。本笔记只做开源对照，不覆盖规格。
> 建立：2026-09-12。随讨论增补，不把猜想写进「可照搬」。

---

## 0. 怎么用

1. 先看 §1 锚点，确认这次讨论没有漂回 RAG / 聊天框 / 给人看的库。
2. 用 §2 把话题映射到章节，只精读映射到的那几段。
3. 需要核对原文时，用该节的 **码源** 打开，不要凭记忆发挥。
4. 「可照搬」= 逻辑层同意拿来用。「可优化」= 他们的做法有缺陷，或和我们已锁定方向打架，讨论时优先想这里。
5. 规则怎么披露、工具叫什么、目录怎么划，**仍未定**。笔记里出现的工具名都是别人的，不是我们的接口。

---

## 1. 我们的锚点（对照别人时用）

- 本机 MCP，完全给 Agent 用；人点名启用，人不用看库。
- 常驻资料室。用户把想放的资料放进去，补的是参数知识和临时联网之外那一层。
- 两个动作：检索、写回（人要求才写）。
- 正本是文档（Markdown），不是向量块。
- 拟人：先定位材料，再决定要不要深读。找不逛总目录；找和读分开；**规则由代码执行**。
- 以后：Agent 写的东西能新替旧。做法未定。

别人如果优化的是「尽快把相似段落塞进上下文」，那一节只当反例。

---

## 2. 讨论主题 → 先看哪里

以后无论聊到哪，先落到这一行，再打开对应节。没有行的，先补映射再讨论。

| 以后要讨论的 | 先读 | 码源优先 |
|---|---|---|
| 现行规格（六扇门） | `需求.md` | 已收口 |
| 白话步骤 | `docs/功能流程.md` | 给人看 |
| 找和读怎么分开、第一次返回什么 | §4.1 §4.2 §3.1 §3.2 | OKF `search.go`；BM `search.py` 的 compact |
| 搜索要不要带摘录 | §4.2 §3.3 | mcpvault `types.ts` 的 `ex` 字段（反例） |
| 长文怎么读一段、要不要大纲 | §4.1 §3.3 | mcpvault `get_note_outline` / `read_note_lines` |
| 写回、何时写、写什么 | §4.3 §3.1 | OKF CONVENTION §6–§10、§18 |
| 新替旧、冲突、要不要留历史 | §4.3 | OKF CONVENTION §13–§14（**和用户「旧的不要」有张力**） |
| 规则放代码还是放 skill/约定 | §4.5 | OKF CONVENTION §16、§18 |
| 文件长什么样（frontmatter、身份、标签） | §4.4 | OKF `types.go`；BM `NOTE-FORMAT.md` |
| MCP 几个工具、参数别名、硬上限 | §4.6 | BM `search.py` 别名；mcpvault limit；ContextNest 工具膨胀（反例） |
| 中文检索、词对不上 | §4.7 | OKF `search.go` tokenize |
| 一次读多块、部分失败、总预算 | §4.1 §4.6 `需求.md` 4.1 | mcpvault `read_multiple_notes`（借 string[]+部分失败，不借无总顶、不借新工具）；ContextNest 只借预算 |
| 写笔记求证 | `需求.md` 4.4；`docs/写笔记求证.md` | 本库闸门。OKF 的 verified 只是 SHOULD，不抄 |
| 书/长文 vs 短笔记（单位是什么） | §4.8 | OKF `examples/books`（他们是书的卡片，不是书） |
| 导入 PDF/EPUB/网址 | §3.5 §3.2 | markitdown；BM `document_ingestion/` |
| 要不要向量 / 多库路由 | §1 §3.6 `需求.md`；**上 RAG 先看 `docs/归档/2026-09-14-调研-三项目与检索层演进.md`** | 不要从 RAG MCP 开干 |
| 块内导航 / 命中分布 / 冷启动成本 | `docs/归档/2026-09-14-调研-三项目与检索层演进.md` §1 §3 | 真库实测数字 |
| 许可证、能不能当底座 | 各项目「不能当底座」 | AGPL vs MIT |
| Agent 行为（点名启用之后它自己找） | §4.5 | OKF 把「何时用」放 skill，把「合法读写」放工具 |

---

## 3. 项目卡片

### 3.1 OKF Agent Memory — 第一对照

**来源**

- 仓库：https://github.com/okf-memory/okf-agent-memory （MIT，Go，约 0.6k star）
- 约定全文：`docs/CONVENTION.md`
- 搜索实现：`pkg/okf/search.go`（`SearchResult` 无 Body）
- 类型：`pkg/okf/types.go`（一份资料 = 一个 Concept）
- MCP：`cmd/okf/mcp.go`（`okf_search` / `okf_show` / `okf_create` / `okf_update` / `okf_relate` / `okf_validate`）
- 分层索引样例：`knowledge/index.md`
- 「书」样例：`examples/books/`（注意：是书的元数据卡片，不是书的正文）
- 上游格式：Google OKF v0.2（仓库 README 链到 `GoogleCloudPlatform/knowledge-catalog`）

**逻辑层可照搬**

- **找和看是两个工具。** `search` 返回 `concept_id, title, type, description, score, matched_on, tags, 链接`，不含正文；要正文走 `show`。这是目前最接近「路标 / 精读」的现成切法。
- **发现路径写成四步**（CONVENTION §16）：轻量索引/搜索 → 候选概念 → 按需打开 → 需要时再沿链接走。禁止整库灌进上下文、禁止无目的 `list_dir` / 全库 grep。
- **检索打分有字段权重**：title ×4、tags ×3.5、description ×2.5，再才是正文。路标质量主要靠标题和一句 description，不靠切段。
- **Search-before-write**：写入前必须先搜有没有同一主题，优先改旧的，不复制一份。
- **工具 vs Agent 分工**（CONVENTION §18）：Agent 决定「值不值得记、是不是同一条、要不要更新」；工具负责合法文件、ID、校验、搜索。Skill 只写「何时来、怎么走」。和我们「规则由代码执行」同方向。
- **不要人工切碎**（§5.1）：能作为一份完整意思的，不要拆成一堆碎片。独立生命周期的才拆。这是反 RAG 切块的现成表述。
- **写什么 / 不写什么**（§6–§7）：只留对未来的人或 Agent 有用的；不进库的包括闲聊、思维链、暂时推理、重复拷贝。
- **身份与来源**：`type / title / description / tags / status / sources / generated / verified`。Agent 生成的和人核实过的必须分开。
- **校验是工具的活**：写完可以 `validate`，不靠模型保证格式。

**不能当底座 / 不要误抄**

- 产品是**项目里的短概念记忆**（决策、事实），不是用户丢进去的长书长文。`examples/books` 只有书名+几句概述。
- 渐进披露大量写在 **Convention（Agent SHOULD）** 里，只有「search 不带正文」是代码硬约束。我们要代码执行，不能只抄一份 skill 文档。
- 新替旧：他们 **禁止静默覆盖**，要留历史、`status` / `stale_after`，过期不自动删。用户说过「旧的不要存在了」——这是冲突，不能直接照搬 §13–§14。
- BM25 按空白/字母切词，中文长句容易变成一个大 token，中文书房会弱。
- 概念图（relate）不是我们现在的产品。

**可优化（相对他们，为我们）**

- 把 §16 的「不准整库灌、search 只给元数据」**全部做成工具行为**，不依赖 Agent 自觉。
- 资料单位要能覆盖**长文的一章**，不是只有几百字的 concept。索引可以学 `index.md` 一句 description，正文另存。
- 写回：工具层可强制「先搜再写」或「同一身份则更新」，减少重复；**覆盖策略单独讨论**（见 §4.3），不要默认 OKF 的「永远留历史」。
- 中文检索要单独设计，不能直接用他们的 `tokenize`。

---

### 3.2 Basic Memory — 成熟 MCP 对照

**来源**

- 仓库：https://github.com/basicmachines-co/basic-memory （AGPL-3.0，Python ≥3.12，约 3.9k star）
- 笔记格式：`NOTE-FORMAT.md`
- 搜索去正文：`src/basic_memory/mcp/tools/search.py` → `_compact_search_response`
- 读：`read_note.py` / `view_note.py`；写：`write_note.py` / `edit_note.py`
- 工具目录：`src/basic_memory/mcp/tools/`
- 文档导入：`src/basic_memory/document_ingestion/`（有 PDF）
- 参数别名：`search.py` 里 `query/q/search/text`、`page_size/limit/per_page`

**逻辑层可照搬**

- **搜索结果故意去掉正文。** 注释原文：*Keep discovery identities and pagination without repeating source prose.* `content` / `matched_chunk` 置空，只留身份和路径。和 OKF search 同一逻辑，而且是从「曾经返回摘录」改过来的，说明这是踩坑后的结论。
- **文件是正本，库是派生的。** Markdown 改了，索引跟着更新。知识不关在数据库里。
- **稳定身份**：`permalink`，文件挪了还能指到同一份。
- **frontmatter 可检索**：title / type / tags + 自定义字段。
- **MCP 参数给模型别名**：模型会乱叫 `q` / `limit` / `note_type`，工具侧兼容，避免调用失败。
- 本机 MCP、多 Agent 共用同一份 Markdown。

**不能当底座 / 不要误抄**

- AGPL：本机自己跑可以；改了当自己的产品分发不行。Python 要 3.12+，当前环境是 3.11.4。
- 产品是 Zettelkasten + 知识图谱（Observations / Relations），单位是卡片事实，不是章、不是书。
- 已加上语义检索 / hybrid / rerank，主路径在往 RAG 靠。我们不跟。
- 工具很多（search / read / view / write / edit / move / delete / build_context / list_directory / project…），Agent 会挑花。`build_context` 会沿图谱拉一圈，容易一次塞太多。

**可优化（相对他们，为我们）**

- 去正文的 compact **可以更彻底**：连 observation 标签里的摘录也不要；返回「哪本/哪章/为什么像」而不是图谱节点。
- 工具要少，两个动作（找、写）+ 读如果需要就单独一个；不要 15+ 个平级工具。
- 导入 PDF 的方向对，但应变成「进资料室的长文」，不是拆成 observation 列表。
- 不要默认上向量；他们的 semantic search 当「以后可选项」看，不当现在的形态。

---

### 3.3 MCPVault — 文件层近亲

**来源**

- 仓库：https://github.com/bitbonsai/mcpvault （MIT，TypeScript，约 1.7k star）
- 工具注册：`src/createServer.ts`
- 搜索：`src/search.ts`（默认 limit=5，硬顶 20）
- 结果形状：`src/types.ts` → `SearchResult { p, t, ex, mc, ln }`（`ex` = excerpt）

**逻辑层可照搬**

- **大纲再按行读**：`get_note_outline`（标题+层级+行号，不读全文）→ `read_note_lines`（只读区间）。描述里写死：先 outline，再按需读段。这是长文件上的渐进披露，和「书/章」同构。
- **结果数量硬上限**写在代码里，不靠模型少要。
- 写/删等破坏性操作要路径确认（`confirmPath`），防 Agent 写错地方。
- 可以指到任意 Markdown 文件夹，不必真用 Obsidian。

**不能当底座 / 不要误抄**

- 是保险柜，不是资料室：没有「资料」身份，没有导读，没有写回策略。
- **`search_notes` 返回摘录 `ex`。** 这是标准「搜索即塞段落」，和已锁定方向相反，当反例。
- 工具仍然偏多（读/写/补丁/多篇读/标签/frontmatter/wiki_link…）。
- 搜索是扫全部 md 再打分，库一大就笨。

**可优化（相对他们，为我们）**

- 把 outline→按段读收到「读」里，不要和「找」混成返回摘录的 search。
- 找的结果学 OKF/BM：路径+标题+一句理由，不要 `ex`。
- 硬上限保留；破坏性写确认保留。

---

### 3.4 ContextNest — 预消化对照（嫩，只借思路）

**来源**

- 仓库：https://github.com/PromptOwl/ContextNest （AGPL，约 15 star）
- MCP 说明：`packages/mcp-server/README.md`
- 工具：`context_init` / `context_folders` / `context_list` / `context_search` / `context_get` / `context_import` / `context_resolve`…

**逻辑层可照搬**

- **先看文件夹和清单，不打开正文**：`context_folders`、`context_list`（可按 folder，禁止默认爬整库）。
- **进口**：`context_import` 一次收一整个 Markdown 文件夹。
- **按需取正文**，和 list 分开。
- 他们把「摘要、hub 文档」当预消化，让下次少读原文——和「导读」是同一类想法（导读怎么写仍未定）。

**不能当底座 / 不要误抄**

- 太新、AGPL、38 个工具、偏审计/版本哈希/市场发布，和个人资料室不是同一个产品。
- `context_resolve`「在 token 预算内返回全文」仍可能一次倒很多正文。
- 不要学工具数量。

**可优化**

- 预消化如果做，应是资料自己的一层说明，不是另一套平台格式。
- 工具保持极少；list/overview 若要有，也是「找」的一种输入，不是第三个产品。

---

### 3.5 零件：MarkItDown

**来源**

- 本机 skill：`~/.claude/skills/markitdown/scripts/convert.py`（或用 `KNOWLEDGE_MARKITDOWN` 指定）
- 上游：Microsoft MarkItDown（EPUB/PDF/DOCX → Markdown）
- MCP 封装很多（如 `KorigamiK/markitdown_mcp_server`），都只做转换

**可照搬**：进口管道用现成转换，不要自己解析 PDF。  
**不是资料室**：转完之后如何成为可找、可读、可覆盖的一份资料，是我们的事。

---

### 3.6 明确不拿来当参考底座

这些只在有人又提起「要不要 RAG/记忆层」时当反例，日常讨论不必打开：

| 项目 | 为何排除 |
|---|---|
| jeanibarz/knowledge-base-mcp-server、lyonzin/knowledge-rag | 本地切块向量 MCP |
| mem0、getzep/graphiti、doobidoo/mcp-memory-service | 对话记忆 / 图谱记忆，不是用户资料室 |
| khoj-ai/khoj、HKUDS/DeepTutor、HKUDS/LightRAG | 整套 App 或 RAG 引擎；DeepTutor 是客户端 |
| PleasePrompto/notebooklm-mcp | 画面像「丢资料再聊」，但是 Google 云，不是本地 Markdown |
| cyanheads/obsidian-mcp-server、MarkusPfundstein/mcp-obsidian | 和 mcpvault 同类，更绑 Obsidian；看 3.3 即可 |

> **2026-09-14 补充：** 又提起「要不要上 RAG」时，**先看 `docs/归档/2026-09-14-调研-三项目与检索层演进.md`**。
> 那份是三项目（QMD / LightRAG / RAPTOR）的深读 + 真库实测，已经把这些反例验证过一遍，
> 并给出「该抄哪一条、什么条件下才该上向量」的可判定触发条件。不要凭本表的旧结论讨论。

---

## 4. 横切：以后会反复碰到的逻辑

### 4.1 找和读分开

三家不约而同（实现深浅不同）：

- OKF：`search` → `show`
- BM：`search_notes`（compact）→ `read_note`
- mcpvault：`get_note_outline` → `read_note_lines`（文件内部）

可照搬的逻辑：**定位 和 取正文 不是同一次返回。**  
我们已锁定「规则由代码执行」：工具不把全文/摘录泼回去，比 OKF 只写 SHOULD 更硬。

可优化：别人把「库内找哪一份」和「一份之内读哪一段」拆成很多工具。我们可以以后再定，是两个动作还是三个，但不要学成 15 个。

### 4.2 搜索返回什么

| | 返回 | 态度 |
|---|---|---|
| OKF | id/标题/一句描述/命中字段，无正文 | 可照搬 |
| BM compact | 身份和路径，正文置空 | 可照搬 |
| mcpvault | 路径+标题+**摘录** | 反例 |

可优化：路标里要有「为什么像」（OKF 的 `matched_on` 是字段名，不是理由）。理由怎么生成未定，但应能让 Agent 否决「不是这个」。

### 4.3 写回与新替旧

OKF 最完整，且和用户原话有张力，讨论写回时必看：

- 人要求才写 ↔ 他们还有「大事之后主动 review」。我们已锁：人要求才写。主动沉淀是以后的事。
- Search-before-write、优先更新同一条：可照搬逻辑。
- 「冲突时不准静默覆盖、要留历史」↔ 用户「新的取代旧的，旧的不要」。**这是产品选择，不能偷渡 OKF 的历史观。**
- `generated` vs `verified`：Agent 自己写的要能标出来，方便以后清理——这个可留。
- 不写闲聊和思维链：可照搬。

可优化：覆盖/归档/留历史，做成工具上的明确策略，不让模型自己决定「差不多就算一条新的」。

### 4.4 正本与元数据

可照搬：一份资料 = 一个 Markdown + YAML 头（标题、类型、标签、一句描述、来源、状态）。文件是正本。

可优化：

- 长文不能整本塞进一个 concept body 再靠 BM25；结构（章）怎么保留未定，但单位不能是向量块。
- OKF 的书样例只有卡片——我们若只抄卡片，矛盾论只能摸到书名摸不到篇。

### 4.5 规则在代码 vs 在约定

OKF 第 18 节分三层：Agent 判断语义；Skill 写何时来；工具保证合法。

我们比他们更硬的那一句：**披露规则由代码执行。** Skill 可以告诉 Agent「该来了」，但不能指望它自觉少读。

可优化：凡是「不准整库、不准 search 带正文、一次读有上限」都放工具；凡是「这个话题要不要来库里」放 skill（人点名启用之后）。

### 4.6 工具个数、别名、上限

- 少工具：OKF 六个已经偏多，但 search/show/create/update 的切法清楚。
- ContextNest 38 个、BM 一长串、mcpvault 十几个：反例。
- 别名：照搬 BM，模型乱参也能命中。
- 硬上限：照搬 mcpvault 的 limit cap、OKF 的 MaxSearchLimit=100（我们可能更小）。

### 4.7 中文检索

OKF tokenize 对中文不友好。资料室大量中文书时，关键词策略必须另做（字 n-gram、或先靠标题/导读）。这是实现时的优化点，现在不定方案。

「词对不上」以后若真痛，向量只作为找路标的加速器（见 `需求.md`），不改两个动作。

### 4.8 资料单位：短概念 vs 长文

这是我们和所有对照项目的真正差别。讨论「一份资料是什么」时看这里。

- OKF/BM：短、自含、靠链接拼起来。
- 我们：用户会丢书、长文、网页、笔记。书要能被摸到篇/章，不能只有一张卡片。
- 可照搬：不要为了检索把意思切碎（OKF 第 5.1 节）。
- 可优化：索引层可以像 OKF 一样「一行一个标题+一句描述」；正文层必须还能按结构读。怎么分层未定。

---

## 5. 优化清单（讨论时当待选项，不是已定方案）

相对这些项目，我们若自己做薄 MCP，值得记住的优化方向：

1. **search 不带正文**做成唯一合法找法（OKF/BM 已做，mcpvault 没做）。
2. **披露限制全在工具**，不靠 Convention 文档（比 OKF 硬）。
3. **工具极少**，贴近「检索 / 写回」，读若需要则单独，避免工具爆炸。
4. **长文可定位到结构**（学 mcpvault outline+行，但不在 search 里给摘录）。
5. **中文检索**不抄 OKF tokenize。
6. **新替旧策略显式化**，不默认「留全部历史」或「静默覆盖」。
7. **Agent 写的带 generated 标记**，便于以后清理。
8. **参数别名 + 结果条数硬顶**。
9. **进口用 markitdown**，不要自研解析。
10. **正本永远是 Markdown 文件**，索引可重建。

---

## 6. 码源速查

| 要核对 | 打开 |
|---|---|
| 找/看分开、结果字段 | https://github.com/okf-memory/okf-agent-memory/blob/main/pkg/okf/search.go |
| MCP 工具表 | https://github.com/okf-memory/okf-agent-memory/blob/main/cmd/okf/mcp.go |
| 约定（写回、披露、分工） | https://github.com/okf-memory/okf-agent-memory/blob/main/docs/CONVENTION.md |
| 索引长什么样 | https://github.com/okf-memory/okf-agent-memory/blob/main/knowledge/index.md |
| 他们所谓的「书」 | https://github.com/okf-memory/okf-agent-memory/tree/main/examples/books |
| 搜索去正文 | https://github.com/basicmachines-co/basic-memory/blob/main/src/basic_memory/mcp/tools/search.py |
| 笔记格式 | https://github.com/basicmachines-co/basic-memory/blob/main/NOTE-FORMAT.md |
| 大纲+按行读 | https://github.com/bitbonsai/mcpvault/blob/main/src/createServer.ts （搜 get_note_outline） |
| 搜索带摘录（反例） | https://github.com/bitbonsai/mcpvault/blob/main/src/types.ts |
| 预消化/少打开正文 | https://github.com/PromptOwl/ContextNest/blob/main/packages/mcp-server/README.md |

---

## 修订

| 日期 | 改了什么 |
|---|---|
| 2026-09-14 | 指向新调研：`docs/归档/2026-09-14-调研-三项目与检索层演进.md`（QMD/LightRAG/RAPTOR 深读 + 真库实测 + RAG 复访条件）。§2 加映射行。 |
| 2026-09-13 | 补映射：一次读多块、写笔记求证。现行规则仍以 `需求.md` 为准。 |
| 2026-09-12 | 初稿。对照 OKF / basic-memory / mcpvault / ContextNest / markitdown。建立讨论映射表。 |
