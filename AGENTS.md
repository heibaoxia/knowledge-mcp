# 给后来 Agent 的说明

先读，再动手。不要凭记忆发挥。

## 每次开工读这些

1. `需求.md` — 现行规格（以它为准）
2. `docs/功能流程.md` — 每一步是人 / Agent / 代码谁干
3. `docs/计划.md` — 先做哪一块、怎样算做完
4. 若改检索、写回、净化、披露：再读 `参考笔记.md` 的映射表，需要时打开里面的码源

旧讨论稿在 `docs/归档/`，不再生效。

## 这是什么

本机 MCP，给 Agent 用的资料室。正本是 Markdown。不做人的笔记软件，不当 RAG。

代码焊步骤，Agent 干活。卡壳把失败交还 Agent，不许拆闸门抄近路。

## 目录

```
原始资料/          人丢 PDF/EPUB/MOBI
原始资料归档/      转换后的源文件（不检索）
资料/书/           书，一部一个目录，门口 导读.md
资料/笔记/         笔记，一份一个 md
检修/提案/         自检写出的改功能提案
mcp/               本工具代码
docs/              规格与流程
```

检索只打 `资料/`。主题不当文件夹。

## 六扇门（能力不能少，函数名可改）

1. 检索 — 先抬头后正文；书+笔记一起搜
2. 收件箱入库 — `原始资料/` 里全部待处理源文件
3. 指定入库 — 只转点名的源文件（文件路径，不是书名）
4. 写笔记 — 必须先搜类似条目；只能进笔记；动不了书
5. 净化笔记 — 先清单后动手；只动笔记
6. 自检 — 交使用/失败记录 + 建议；第一版不准直接改本工具源码

## 写代码时不许做的

- 把向量库 / 数据库当正本
- 按学科拆多个库再路由
- 查询一次返回全书或无限摘录
- 净化或写笔记改书
- 后台定时偷偷入库、净化、改自己
- 工具自己去找书、下载书
- 新增一堆平级工具把六扇门拆碎
- 不问规格自己加产品能力

## 实现偏好

轻量：stdio MCP、少依赖、正本就是文件。转换用现成 markitdown，不自研 PDF 解析。
