# 给后来 Agent 的说明

先读，再动手。不要凭记忆发挥。

## 每次开工读这些

1. `需求.md` — 现行规格（以它为准）
2. `docs/功能流程.md` — 每一步是人 / Agent / 代码谁干
3. `docs/计划.md` — 先做哪一块、怎样算做完
4. `docs/问题.md` — 还没开 Spike 的类问题（对策必须对类生效）
5. `docs/spike/` — 进行中的本轮 Spec / 调研 / 计划；**没有进行中的稿就不要造**
6. `docs/验收标准.md` — **冻结的验收标准**：题集 Q1–Q14、取词回捞 W1–W20。**不许因实现麻烦放宽**
7. 若改检索、写回、净化、披露：再读 `参考笔记.md` 的映射表，需要时打开里面的码源

旧讨论稿在 `docs/归档/`，不再生效。

## 这是什么

本机 MCP，给 Agent 用的资料室。正本是 Markdown。不做人的笔记软件，不当 RAG。

代码焊步骤，Agent 干活。卡壳把失败交还 Agent，不许拆闸门抄近路。

## 目录

```
原始资料/          人丢 PDF/EPUB/MOBI
原始资料归档/      转换后的源文件（不检索）
资料/书/           书，一部一个目录：导读.md（来自书）+ 地图.md（派生）+ 各块
资料/笔记/         笔记，一份一个 md
检修/提案/         自检写出的改功能提案
mcp/               本工具代码
docs/              流程、验收、问题清单、进行中的 spike/、已完成归档
```

检索只打 `资料/`。主题不当文件夹。

## 六扇门（能力不能少，函数名可改）

1. 检索 — **只回身份和命中理由，永不回正文**；**两路并行**，路标三栏：两路都中 / 仅字面 / 仅地图（重合由代码算）；加深一次可多块（有顶）；书+笔记一起搜。阅读超长篇用显式 `#n` 窗口
2. 收件箱入库 — 转切块 + **合格地图** + 索引失效
3. 指定入库 — 只转点名的源文件（文件路径，不是书名）
4. 写笔记 — 预览 → 求证 → 写入；只能进笔记；动不了书
5. 净化 — 先清单后动手；动笔记和地图，不动书正文
6. 自检 — 交使用/失败记录 + 建议；第一版不准直接改本工具源码

## 改代码的原则（问题类，不是单题）

**禁止为某一道验收题、某一本书、某一个问句写规则或过滤器。**  
只改「这一类输入 / 这一类问句」的行为。用合成库闸门钉住这一类；真库题只做抽查，不许对着漏了的那本再加黑名单、停用词、生活题偏方。上一轮为挡毛选加问句过滤、为「讲了什么」按字面啃掉「讲」，都是针对性过强的反例。

文档也按这个节奏：`需求.md` 是唯一现行规格；进行中的 Spike 在 `docs/spike/`；做完整包进 `docs/归档/`；还没开 Spike 的类问题写在 `docs/问题.md`。

## 写代码时不许做的

- 把向量库 / 数据库当正本
- 把索引落盘成文件或数据库（索引只在进程内存里活，进程退出即消失）
- 让工具替 Agent 说「你读够了」（只许报还有多少、哪些已读，判断归 Agent）
- 用摘要替换书名/章名；地图可以生成但不能当原文、不能当唯一检索面
- 两路结果丢给 Agent 自己对名单（重合必须代码标出）
- 引「翻页游标」这类会话内隐藏状态
- 只按一级标题切块（必须认到三级标题；有 `#` 时不要用「一、」撕篇）
- 预切 4K 小文件、或把「关系/分析」当停用词
- 零命中时回退成抬头检索硬凑
- 用 24 小时 `calls.jsonl` 给路标打已读（会串会话）
- 按学科拆多个库再路由
- 查询一次返回全书或无限摘录；搜索带正文摘录；按问句自动灌命中章
- 为一次读多块或求证新开平级工具
- 净化或写笔记改书
- 后台定时偷偷入库、净化、改自己
- 工具自己去找书、下载书
- 新增一堆平级工具把六扇门拆碎
- 不问规格自己加产品能力
- 为单题、单书写检索/入库特例（见上方「问题类」）

## 实现偏好

轻量：stdio MCP、少依赖、正本就是文件。转换用现成 markitdown，不自研 PDF 解析。

索引：Python 标准库自带的 SQLite FTS5，**内存模式**（不落盘、零新依赖）。中文索引切二字片+单字；查询片内相邻、禁止整句碎片 OR、片间不必全 AND，不上 jieba。

## 写代码怎么下手（马尾辫 full）

默认按马尾辫（ponytail）**full**：最短能用的改动，先复用本仓库和标准库，不新加依赖，不预留抽象，不另起文件堆脚手架。本机有 ponytail skill 就用；没有就按这几条做，不必去装。不要 ultra——规格已经拍板的，不许再拿 YAGNI 砍。

`需求.md` 写死的步骤、失败交还、pytest 闸门、书不改、检索不带正文、自然篇 + 8000 窗、**地图派生、两路路标三栏**、**`docs/验收标准.md` 冻结题集**：不准偷懒拆掉。

线上跑题后把占用/耗时/轮次**追加**到 `检修/验收记录.md`，不许改期望就题。
