# 给后来 Agent 的说明

先读，再动手。不要凭记忆发挥。

## 每次开工读这些

1. `需求.md` — 现行规格（以它为准）
2. `docs/功能流程.md` — 每一步是人 / Agent / 代码谁干
3. `docs/计划.md` — 先做哪一块、怎样算做完
4. `docs/问题.md` — 还没开做的类问题（对策必须对类生效）
5. `docs/spike/` — **Spike（探路）**：调研/只读实验；**不改代码**。没有进行中的稿就空着
6. `docs/本轮Spec.md` — **有才改 `mcp/`**。实现规格。做完归档，不留空壳。现在没有进行中的轮次就没有这个文件
7. `docs/验收标准.md` — **冻结的验收标准**：题集 Q1–Q14、取词回捞 W1–W20、**第九节三维打分（准/全/省）**。**不许因实现麻烦放宽**
8. 若改检索、写回、净化、披露：再读 `参考笔记.md` 的映射表，需要时打开里面的码源
9. 若本轮要改 `mcp/` 的检索 / 索引 / 入库 / 路标 / 阅读窗：先把第九节「做完门槛」看完。没有打分节不许说做完

旧讨论稿在 `docs/归档/`，不再生效。

## 这是什么

本机 MCP，给 Agent 用的资料室。正本是 Markdown。不做人的笔记软件，不当 RAG。

代码焊步骤，Agent 干活。卡壳把失败交还 Agent，不许拆闸门抄近路。

## 目录

```
原始资料/          人丢 PDF/EPUB/MOBI
原始资料归档/      转换后的源文件（不检索）
资料/书/           书，一部一个目录：导读.md（来自书）+ 地图.md（派生）+ 各块
资料/笔记/         笔记，一份一个 md
检修/提案/         自检写出的改功能提案
检修/验收记录.md   线上题逐题明细（只追加）
检修/打分记录.md   三维打分流水账（只追加，打开即历史）
mcp/               本工具代码
docs/              流程、验收、问题清单、spike（探路）、本轮Spec（有才改代码）、归档
```

检索只打 `资料/`。主题不当文件夹。

## 六扇门（能力不能少，函数名可改）

1. 检索 — **只回身份和命中理由，永不回正文**；**两路并行**，路标三栏：两路都中 / 仅字面 / 仅地图（重合由代码算）；加深一次可多块（有顶）；书+笔记一起搜。阅读超长篇用显式 `#n` 窗口
2. 收件箱入库 — 转切块 + **合格地图** + 索引失效
3. 指定入库 — 只转点名的源文件（文件路径，不是书名）
4. 写笔记 — 预览 → 求证 → 写入；只能进笔记；动不了书
5. 净化 — 先清单后动手；动笔记和地图，不动书正文
6. 自检 — 交使用/失败记录 + 建议；第一版不准直接改本工具源码

## 改代码的原则（问题类，不是单题）

**禁止为某一道验收题、某一本书、某一个问句写规则或过滤器。**  
只改「这一类输入 / 这一类问句」的行为。用合成库闸门钉住这一类。真库 **Q/W 全组是打分对照**（见下方「做完门槛」），不是对着漏了的那本加补丁：不许加书名黑名单、停用词、生活题偏方。上一轮为挡毛选加问句过滤、为「讲了什么」按字面啃掉「讲」，都是针对性过强的反例。

文档：`需求.md` 唯一现行规格。**Spike**（`docs/spike/`）是探路，不改代码。**本轮 Spec**（`docs/本轮Spec.md`）是实现规格，有它才能改 `mcp/`。做完整包进 `docs/归档/`。类问题在 `docs/问题.md`。

## 写代码时不许做的

- 把向量库 / 数据库当正本
- 把索引落盘成文件或数据库（索引只在进程内存里活，进程退出即消失）
- 让工具替 Agent 说「你读够了」（只许报还有多少、哪些已读，判断归 Agent）
- 用摘要替换书名/章名；地图可以生成但不能当原文、不能当唯一检索面
- 两路结果丢给 Agent 自己对名单（重合必须代码标出）
- 引「翻页游标」这类会话内隐藏状态
- 只按一级标题切块（必须认到三级标题；有 `#` 时不要用「一、」撕篇）
- 预切 4K 小文件、或把「关系/分析」当停用词
- 零命中时回退成抬头检索硬凑
- 用 24 小时 `calls.jsonl` 给路标打已读（会串会话）
- 按学科拆多个库再路由
- 查询一次返回全书或无限摘录；搜索带正文摘录；按问句自动灌命中章
- 为一次读多块或求证新开平级工具
- 净化或写笔记改书
- 后台定时偷偷入库、净化、改自己
- 工具自己去找书、下载书
- 新增一堆平级工具把六扇门拆碎
- 不问规格自己加产品能力
- 为单题、单书写检索/入库特例（见上方「问题类」）

## 实现偏好

轻量：stdio MCP、少依赖、正本就是文件。转换用现成 markitdown，不自研 PDF 解析。

索引：Python 标准库自带的 SQLite FTS5，**内存模式**（不落盘、零新依赖）。中文索引切二字片+单字；查询片内相邻、禁止整句碎片 OR、片间不必全 AND，不上 jieba。

## 写代码怎么下手（马尾辫 full）

默认按马尾辫（ponytail）**full**：最短能用的改动，先复用本仓库和标准库，不新加依赖，不预留抽象，不另起文件堆脚手架。本机有 ponytail skill 就用；没有就按这几条做，不必去装。不要 ultra——规格已经拍板的，不许再拿 YAGNI 砍。

`需求.md` 写死的步骤、失败交还、pytest 闸门、书不改、检索不带正文、自然篇 + 8000 窗、**地图派生、两路路标三栏**、**`docs/验收标准.md` 冻结题集与三维打分**：不准偷懒拆掉。

## 做完门槛（打分）

凡改了 `mcp/` 且触及检索、索引、入库切块、路标格式、阅读窗、地图生成：

1. pytest 全绿（合成库闸门）
2. 真库机械跑 **Q1–Q14 全组 + W1–W16 全组**（不许抽 5 题/5 词当全绿）
3. 按 `docs/验收标准.md` **第九节**打 **准 / 全 / 省**（三个数并排，不合成总分）
4. **追加明细** `检修/验收记录.md` 一节：书本数、块数、占用列、三维汇总、对照上轮。第 5 轮起用该文件顶部模板
5. **在 `检修/打分记录.md` 文末追加一节**（流水账，不改旧节）：准/全/省、书/块、代码短哈希、对照上轮。打开该文件往下读就是历史。

没有验收记录新节、**没有打分流水新节**：**不许说本轮做完**，不许把 `docs/本轮Spec.md` 归档删掉。不许改期望就题。不许为刷「准」加书名黑名单。

只改写笔记 / 净化 / 自检、没碰 retrieve/index/ingest：pytest 对应闸门即可，不强制线上三维。
