# 个人知识库 MCP · 需求规格

> 立项：2026-09-12  
> 位置：`F:\project\knowledge\`  
> 状态：**用户需求已收口，本文为现行规格**  
> 旧稿已归档：`docs/归档/2026-09-12-需求-立项讨论稿.md`  
> 步骤白话版（给人核对用）：`docs/功能流程.md`  
> 开源对照：`参考笔记.md`（讨论实现细节前先看映射表）

本文写的是**要做成什么、怎么用、规则是什么**。  
工具对外叫什么名字、用哪套 MCP 框架，实现时再定，不挡开工。

---

## 1. 是什么

给 Agent 用的**本机 MCP**。人把资料放进来，任何接了这个 MCP 的 Agent 都可以从里面找东西、往里面收书、写笔记、整理笔记。

它补的是 Agent 知识来源里缺的那一层：

| 途径 | 特点 |
|---|---|
| 训练参数 | 永远在，但不是你的材料 |
| 临时联网 | 新，但用完即走 |
| **本库** | 你给的、可积累的材料 |

形态是一间**常驻资料室**，不是一次对话的公文包，不是 RAG 聊天框，不是给人点的笔记软件。

- 使用者是 Agent。人可以打开文件夹看，但不是给人办公的。
- 接上 MCP 之后，Agent 开场就知道有这些能力（和 Skill 不同：不必先「调用才看见说明书」）。你再说「用这个库」，只是提醒它去用。
- 数据属于你：正本永远是普通 Markdown，不属于某个 Agent、某一场对话。
- 轻量实用：不把知识存进数据库；不切向量当正本。

### 1.1 不是什么

- 不替代常识，也不替代联网。库里有的以库为准；没有的照常聊或去搜，不许装查过。
- 不负责找书、下载书、知道你在聊天里选了哪本。
- 不当真 Skill（只借用「抬头很短、正文按需加载」这个形状）。
- 不按学科拆成多个库再智能路由。
- 第一版不自己改自己的程序代码。

---

## 2. 三条死规矩

1. **代码焊步骤，Agent 干活。**  
   必须走哪些步、每一步拿得到什么，由代码执行。跳步、越权直接拒绝。认字、判断、写句子，仍是 Agent。
2. **先抬头，后正文。**  
   检索默认只给路标（哪几份、凭什么像）。要不要正文、要哪一块，Agent 自己判断再点名来取。一次不能把整本灌走。
3. **书和笔记分开存、分开维护、检索打在一起。**  
   书是原文，进库后不改、不净化。笔记更自由，可以改、合并、删。Agent 不必先说「搜书还是搜笔记」。

卡壳时：代码停，把「卡在哪、为什么、已做成什么、正门上还能怎么试」交还 Agent。不在工具里死循环，也不放开闸门让它抄近路。

---

## 3. 东西放哪

人碰的（检索当没看见）：

```
原始资料/            只准往这儿丢 PDF / EPUB / MOBI
原始资料归档/        转成 Markdown 之后，源文件搬到这儿
```

工具只检索：

```
资料/
  书/<slug>/
    导读.md          抬头 + 几句话 + 章节名（发现用，要短）
    各章.md          能按原书一级标题切开就切开；切不开就一篇正文
  笔记/<slug>.md     一份一个文件，抬头同样要有
检修/提案/            自检写出的改功能提案（不参与检索）
mcp/                  本工具代码
```

- `<slug>` 用 ASCII，中文标题写在文件头里。
- 主题（心理学、矛盾论）**不当文件夹**，写在抬头的标签和介绍里。
- 默认这些目录在本仓库下；路径可配置。人的正路只往 `原始资料/` 放。

每份 Markdown 头上至少有：标题、类型（书/笔记）、几句介绍；书和笔记都可以有标签。Agent 写的笔记要能标出来（方便以后清理）。有源文件的写下源文件在归档里的位置。

---

## 4. 六扇门

都由 Agent 点。工具不在后台偷偷入库、偷偷净化、偷偷改自己。

一扇门可以是一次或两次调用（例如检索之后再阅读）。对外能力不能少，函数名实现时再定。

### 4.1 检索

**输入：** 想了解什么（一句话即可）。  
**代码：** 只在 `资料/` 里扫抬头（标题、介绍、标签、书的章节名），不打开正文；留下最像的几条（建议最多 5 条）；返回路标，默认无章节正文。书和笔记一起搜。  
**Agent：** 问什么、要不要加深、跟你怎么说、提不提书名。  
**输出：** 几条路标，或明确没有。

**加深（阅读）：** Agent 点名哪一份哪一块。代码只给那一块，有字数上限；没身份、一次要整本，拒绝。

### 4.2 收件箱入库

**输入：** 「把原始资料里现有的收进来」。  
**代码：** 转换该文件夹里全部待处理源文件 → `资料/书/`（目录 + 导读骨架）；源文件搬到归档；回报成功/失败。  
**Agent：** 只负责点一下，再把结果告诉你。  
**不管：** 文件是谁下载的。

### 4.3 指定入库

**输入：** 一个或多个**源文件路径**（或已在 `原始资料/` 里的文件名）。  
**代码：** 只转这几个，其它待处理的不动；同样进 `资料/书/`、源文件进归档。  
**Agent：** 自己找文件；找不到就告诉你，不要调这一门。指定的是文件不是书名。  
**不管：** 这是不是你聊天里选的 B、C。工具只知道又进了几份合法的书。

书只从源文件转换来。没有源文件却想当书写进 `资料/书/`，拒绝。

### 4.4 写笔记

**输入：** 一段 Markdown（感悟、心得、非书）。  
**代码（跳不过）：** 先用同一套检索把「已经像的条目」扔回去；必须是 Markdown；只能进 `资料/笔记/`；要有标题和几句介绍；动不了书。  
**Agent：** 写什么；看过类似条目之后，决定改旧的还是新开。  
**输出：** 记下了，或拒绝原因。

### 4.5 净化笔记

**输入：** 「整理笔记」。  
**代码：** 先只扫 `资料/笔记/`，列出可疑项（重复、太短、没抬头、过期），**只出清单不删**；Agent 提交处理清单后，只许动这些笔记，碰到书整单拒绝；执行并回报。  
**Agent：** 是不是同一主题、留谁、合并后怎么写。  
**新替旧发生在这里**（以及写笔记时改旧页），不发生在书上。

借鉴卡帕西 LLM Wiki 的，只借在笔记上：笔记会进化；书当原文。查询仍打在书+笔记全体，不是只打笔记。

### 4.6 自检（检修通道）

**输入：** 「最近有没有可优化的」「我想加/减某个能力」。  
**代码：** 交出最近的调用和失败记录（默认大约一周）；不改库、不改闸门、不改程序。  
**Agent：** 读记录，问你问题，给建议；若要改功能，按下面修理规范写提案。

**修理规范（焊死）：**

1. 先写清：要加还是要减、动哪扇门、对「书不改 / 先抬头后正文」有没有风险。  
2. 你点头。  
3. 先改说明书（本文和 `docs/功能流程.md`），再考虑改程序。  

第一版**不准** Agent 直接补丁 MCP 源代码。自检不是后台根据数据自己改自己。

---

## 5. 卡壳交还

任何一门某一步做不下去：

- 停，不要在工具里连着重试。  
- 带出：卡在哪一步、为什么、已经做成了什么、正门上还能怎么试。  
- Agent 拿着说明改输入、换策略、或问你。  
- 禁止：检索失败就灌全书；入库失败就编一本假书；净化失败就直接删文件。  
- 工具自己崩了：也当失败交出去，并说明是工具坏了，需要修。

同一错误要再试，由 Agent 换了输入再调，这样才修得及时。

---

## 6. 典型用法（验收时就按这些走）

1. 你把三本书丢进 `原始资料/`，让 Agent 收件箱入库 → `资料/书/` 出现三套带导读的 Markdown，源文件在归档。  
2. 聊天选出 B、C，Agent 自己找到文件后指定入库 → 只多这两本，收件箱里其它文件不动。  
3. Agent 找不到书 → 告诉你，不调用入库。  
4. 你让它用这个库谈拖延 → 先给路标；它要再读某一章 → 只拿到那一块。  
5. 库里没有 → 如实空，不装。  
6. 你让记下一条感悟 → 先看到类似笔记，再写入 `资料/笔记/`。  
7. 你让整理笔记 → 先清单后动手，书原封不动。  
8. 你问有没有可优化的 → 它根据记录问你、给建议，不私自改工具。

没被问到来源，不要表演「根据《某某》第 n 章」。

---

## 7. 实现原则（轻量）

- 正本是文件。索引如果以后有，必须能从文件重建，不能当第二份真相。  
- 第一版不做向量库、不做数据库当存储。书多到扫抬头都慢了，再考虑可扔掉的加速器。  
- 进口转换用现成工具（本机已有 markitdown），不要自研 PDF 解析。  
- 工具要少。六扇门不要拆成十几二十个平级工具。  
- 模型乱叫参数时尽量兼容常见别名（query/q、limit 等）。  
- 中文检索先走字面/子串命中抬头和章节名；不够再另说，不因此上 RAG。

---

## 8. 环境与资产

| 项 | 说明 |
|---|---|
| 系统 | Windows，PowerShell 5.1，Python 3.11.4 |
| 本仓库 | `F:\project\knowledge\` |
| 可能接入的 Agent | 凡支持 MCP 的均可。`F:\nanobot\` 只是其中之一，不是本产品 |
| 待处理的书 | `F:\BaiduSyncdisk\obsidian\93_书籍馆\原始格式\待处理\` |
| markitdown | `C:\Users\Maxingyu\.claude\skills\markitdown\scripts\convert.py` |
| `F:\rag-system\` | 已有 RAG 服务。**不是本库形态**；仅当将来「找路标」加速才可能接，接口不变 |
| Obsidian 库 | `F:\BaiduSyncdisk\obsidian\`。本知识库不挂进去 |
| 开源对照 | 见 `参考笔记.md`。底座不直接用 basic-memory（AGPL、Python 3.12）或 OKF（短概念记忆），只借逻辑 |

---

## 9. 第一版明确不做

- 向量检索 / 按学科分多个向量库  
- 把书编译成 wiki 之后不再提供原文  
- 定时后台入库、后台净化、后台自改代码  
- 网页形态（以后可加 `资料/网页/`，头和笔记一样）  
- 给人用的搜索页、挂进 Obsidian  
- Agent 无确认地修改本工具源码  

---

## 10. 相关文档

| 文件 | 干什么 |
|---|---|
| 本文 `需求.md` | 现行规格 |
| `AGENTS.md` | 以后写代码的 Agent 先读这个 |
| `docs/功能流程.md` | 每一步谁干（人/Agent/代码），给人看 |
| `docs/计划.md` | 实现顺序与验收 |
| `参考笔记.md` | 开源对照；改检索/写回/净化前看映射表 |
| `README.md` | 给人看的入口 |
| `docs/归档/` | 旧讨论稿，不再生效 |

---

## 修订

| 日期 | 改了什么 |
|---|---|
| 2026-09-12 | 重写为现行用户规格。六扇门、书/笔记分治、代码焊步骤、卡壳交还、自检不改代码。旧稿归档。 |
