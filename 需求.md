# 个人知识库 MCP · 需求规格

> 立项：2026-09-12  
> 位置：`F:\project\knowledge\`  
> 状态：**用户需求已收口，本文为现行规格**  
> 产品代：**第三版**（`3.2.2`，见 `docs/版本.md`）  
> 旧稿已归档：`docs/归档/2026-09-12-需求-立项讨论稿.md`  
> 步骤白话版（给人核对用）：`docs/功能流程.md`  
> 开源对照：`参考笔记.md`（讨论实现细节前先看映射表）

本文写的是**要做成什么、怎么用、规则是什么**。  
工具对外叫什么名字、用哪套 MCP 框架，实现时再定，不挡开工。

---

## 1. 是什么

给 Agent 用的**本机 MCP**。人把资料放进来，任何接了这个 MCP 的 Agent 都可以从里面找东西、往里面收书、写笔记、整理笔记。

它补的是 Agent 知识来源里缺的那一层：

| 途径 | 特点 |
|---|---|
| 训练参数 | 永远在，但不是你的材料 |
| 临时联网 | 新，但用完即走 |
| **本库** | 你给的、可积累的材料 |

形态是一间**常驻资料室**，不是一次对话的公文包，不是 RAG 聊天框，不是给人点的笔记软件。

- 使用者是 Agent。人可以打开文件夹看，但不是给人办公的。
- **Skill 开门，MCP 做闸。** 默认不把六扇门挂进对话，Agent 看不见这些工具。人点名「用资料室」或点名本库 Skill 之后，这场对话才接上 MCP，工具才出现。Skill 只写用法（先检索再点名读）；跳步、灌正文、改书，仍由 MCP **拒绝**，不靠说明书自觉。正文：`docs/Skill-资料室.md`。
- 数据属于你：正本永远是普通 Markdown，不属于某个 Agent、某一场对话。
- 轻量实用：**正本永远是普通 Markdown**，不属于某个数据库、某个 Agent、某场对话；不切向量当正本。**索引是派生物，不是第二份书**：允许落在本机缓存文件（字面 FTS + 章向量），删了或坏了必须能从 `资料/` 重建。禁止把索引或向量库当正本。

### 1.1 不是什么

- 不替代常识，也不替代联网。库里有的以库为准；没有的照常聊或去搜，不许装查过。
- 不负责找书、下载书、知道你在聊天里选了哪本。
- 不用 Skill 代替闸门，也不把资料室做成「只有说明书、没有硬工具」。
- 不按学科拆成多个库再智能路由。
- 第一版不自己改自己的程序代码。

---

## 2. 死规矩

### 2.1 老三条

1. **代码焊步骤，Agent 干活。**  
   必须走哪些步、每一步拿得到什么，由代码执行。跳步、越权直接拒绝。认字、判断、写句子，仍是 Agent。
2. **先抬头，后正文。**  
   检索**只回身份和命中理由，永不回正文**。要不要正文、要哪几块，Agent 自己判断再点名来取。一次不能把整本灌走；一次多块仍有块数顶和总字数顶。  
   （2026-09-13 措辞更新：原话是「只扫抬头、不打开正文」。正文要进索引才搜得到，所以改成描述**输出**而不是描述**读什么**——实质没变，路标里一个字的正文都不许有。）
3. **书和笔记分开存、分开维护、检索打在一起。**  
   书是原文，进库后不改正文、不按章删、不按章净化。整本可以走净化门退出（`withdraw`），源文件仍在归档，以后可以再入。笔记更自由，可以改、合并、删。Agent 不必先说「搜书还是搜笔记」。

### 2.2 新三条（2026-09-13 定）

4. **入库必须配合检索。**  
   书里有的内容，Agent 一定检索得到——**这是入库的验收标准，而且只能由检索来证明**（从书里抽词回捞）。反过来，每个自然篇必须点名可读完：磁盘按书里的标题存成块（至少认到三级标题），阅读用显式窗口（单窗 ≤ 8000 字），禁止隐藏游标。两头缺一，书就成了「死书」：搜得到读不到，或读得到搜不到。
5. **结构只用来命名和导航，不用来决定能不能被找到。**  
   书可能有目录、可能没有；可能按卷分、可能按章分、可能压根没标题。**能不能被搜到不取决于结构**：正文进索引；结构只负责把块命名得像样、把导航做顺。这样切块策略就不再是单点故障——切得不好也还搜得到，只是读起来不顺。
6. **迭代检索：工具给判断的料，不给判断。**  
   Agent 可以任意多次检索、阅读（无轮次上限）。工具负责摆齐：**这本书命中多少块、建议哪几块、邻块是谁**。**但绝不替 Agent 说「你读够了」**——要不要继续，是 Agent 的判断。命中分布只报数字，不铺卷篇清单。已读如要标，只记本进程读过的身份，不拿跨会话日志去标。
7. **地图是派生物，不是第二份书。**  
   入库时编译书级「能解决什么、不解决什么、建议从哪读」，并覆盖主要非导航章名；可含别名与主题词。抬头（书名、章名）仍来自书里，不许用摘要顶替。点名阅读的永远是原文。借卡帕西 Wiki 的 ingest/query/lint，**不借**「编译完就不再提供原文」。
8. **检索字面与语义并行，融合由代码做，只回路标。**  
   **字面**：字段加权的语片 FTS（书名 / 章名 / 别名 / 地图 / 正文）。**语义**：章级向量召回（一块一向量，不是 512-token 的 RAG 窗）。每一次两路都跑；候选池大于路标上限，融合后最多 5 条路标。路标标明来源（两路都中 / 仅字面 / 仅语义）。不「只靠语义」当唯一路径，也不把向量库当正本。同一身份两边都中 → 路标写「两路都中」，Agent 不对两份名单做集合运算。读哪几块、读完够不够，仍是 Agent 的判断。
9. **人点名用本库，默认答案在书或笔记里。**  
   生活题两路都弱时，给最像的结构命中（地图/别名）并标明，不许装没有。拉丁专名完全不在库（如 PyTorch）仍然必须空。

卡壳时：代码停，把「卡在哪、为什么、已做成什么、正门上还能怎么试」交还 Agent。不在工具里死循环，也不放开闸门让它抄近路。

---

## 3. 东西放哪

人碰的（检索当没看见）：

```
原始资料/            只准往这儿丢 PDF / EPUB / MOBI
原始资料归档/        转成 Markdown 之后，源文件搬到这儿
```

工具只检索：

```
资料/
  书/<slug>/
    导读.md          抬头 + 几句话 + 顶层目录（来自书，发现用，要短）
    地图.md          派生物：能解决什么 / 不解决什么 / 建议从哪读（生成，可重建）
    各块.md          一块一个自然篇/章；超 8000 字阅读用显式窗口
  笔记/<slug>.md     一份一个文件，抬头同样要有
检修/提案/            自检写出的改功能提案（不参与检索）
mcp/                  本工具代码
```

- `<slug>` 用 ASCII，中文标题写在文件头里。这条只管书的目录名；章节文件名可以有中文。
- 块文件：`序号-块名.md`。块名优先用书里的标题（剥掉 epub 残渣如 `[\\*](#id0a)`）；实在没有标题，才用该块第一句前 20 字。中文结构行（「第一章 …」「一、」）**只在整篇没有 markdown 标题时**才当切点。中文不许缩成 chapter/空名；同名加序号天然不撞。
- **入库硬要求：块 = 自然篇/章，块名来自书里，不许编；切完字数守恒 ≥99%（对照切块前的转换产物）。** 结构只用来命名和导航，不用来决定能不能被搜到。超长篇不预切 4K 文件，阅读用显式窗口。
- 书名来源优先级：EPUB 元数据 dc:title → 第一个不像版权/扉页的一级标题 → 原始文件名。
- slug 候选先清洗水印/网址/来源尾巴（z-library 之类），清洗后为空退回 book-{hash8}。
- 主题（心理学、矛盾论）**不当文件夹**，写在抬头的标签和介绍里。
- 默认这些目录在本仓库下；路径可配置。人的正路只往 `原始资料/` 放。

每份 Markdown 头上至少有：标题、类型（书/笔记）、几句介绍；书和笔记都可以有标签。Agent 写的笔记要能标出来（方便以后清理）。有源文件的写下源文件在归档里的位置。

---

## 4. 六扇门

都由 Agent 点。工具不在后台偷偷入库、偷偷净化、偷偷改自己。

一扇门可以是几次调用（检索：先路标再阅读；写笔记：先预览再求证再写入）。对外能力不能少，函数名实现时再定。不要为「一次读多块」或「求证」新开平级工具。

### 4.1 检索

**输入：** 想了解什么（一句话即可）。  
**代码：** 在 `资料/` 里检索，**只回身份和命中理由，永不回正文**（最多 5 条路标）。**两路都跑、代码融合**：字面是字段加权的语片 FTS（书名/章名/别名/地图/正文；章名命中权重大于正文路过）；语义是章级向量（一块一向量）。先召回一个大于 5 的候选池，再融合收成最多 5 条路标。路标分三栏——**两路都中（优先）/ 仅字面 / 仅语义**——重合由代码按身份计算。空栏省略。跨多本的问题允许同一条路标单里出现多本，不得为了「只留一本」把相关书挤掉。查询按语片（片内相邻、禁止整句碎片 OR、片间不必全 AND）；别名只许用地图/章名里的专名扩展，禁止把「变化/发生/怎么回事」当证据。拉丁专名不在库则整句空。文件名含「目录 / 目 录 / 索引 / 参考文献 / 术语表」的块正文不进字面索引。地图不当成书的正文块。不上向量库当正本、不上 jieba、搜索不带正文摘录。  
**Agent：** 问什么、要不要加深、勾选哪几块、跟你怎么说、提不提书名。  
**输出：** 几条路标（含可直接点名的身份），或明确没有。

**加深（阅读）：** Agent 点名一份或多份的块。单块仍用 `target` + `part`；多块用身份字符串列表（如 `书/<slug>/<章>`、`笔记/<slug>`），不要对象数组。  
**代码：** 只给点名的那些块；单次最多 5 个身份、总字数顶 24000，**单窗 8000 字**。超长篇用显式窗口身份（`书/<slug>/<块名>#2`，`#1` 与不带 `#` 等价）；块内小节用 `书/<slug>/<块名>#节名` 或 `@偏移`（`#` 后纯数字才是窗）。同一窗再读仍是该窗，要点下一窗就点 `#2` 或 `@下一偏移`。超总顶截断并标明没读到的项。每次阅读末尾附一行**邻块**（上一篇/下一篇，以及同篇上一窗/下一窗，只有名字）和该块**节目录**（标题+偏移，不进路标），让 Agent 自己决定要不要继续；**不引翻页游标**这类隐藏状态。某一块找不到：这一块按失败交还，其它块照给。没身份、或清单里出现一次要整本：整单拒绝。检索默认仍无正文。

### 4.2 收件箱入库

**输入：** 「把原始资料里现有的收进来」。  
**代码：** 转换该文件夹里全部待处理源文件 → `资料/书/`（目录 + 来自书的导读 + **合格地图** + 各块）；源文件搬到归档；索引失效；回报成功/失败。缺地图、或地图几乎不覆盖章名，不算这本入完。转换产物几乎没有可检索正文（无文字层）→ 这本失败，源文件留在原处，不 OCR。空心 slug（剥完只剩数字和横线）走 `book-{hash8}`。超长块 / 同名章撞车 / 乱码文件名在回报里标未入完，仍落盘。  
**Agent：** 只负责点一下，再把结果告诉你。  
**不管：** 文件是谁下载的。

### 4.3 指定入库

**输入：** 一个或多个**源文件路径**（或已在 `原始资料/` 里的文件名）。  
**代码：** 只转这几个，其它待处理的不动；同样进 `资料/书/`（含合格地图）、源文件进归档、索引失效。  
**Agent：** 自己找文件；找不到就告诉你，不要调这一门。指定的是文件不是书名。  
**不管：** 这是不是你聊天里选的 B、C。工具只知道又进了几份合法的书。

书只从源文件转换来。没有源文件却想当书写进 `资料/书/`，拒绝。

### 4.4 写笔记

**输入：** 一段 Markdown（感悟、心得、非书）。  
**代码（跳不过）：**

1. **预览。** 先用同一套检索把「已经像的条目」扔回去。不预览就写入，不允许。
2. **求证。** 稿上必须有求证结论，四选一：`相符` / `部分不符` / `库中无` / `非事实`。  
   - `相符`、`部分不符`：必须列出依据身份；每个身份真实存在；调用日志显示这些身份已经成功阅读过。没读过就声称相符或部分不符，拒绝。  
   - `库中无`：来源可空；不许写成「根据《某某》」。  
   - `非事实`：不强制阅读（感悟、判断走这条）；若填了来源，身份必须真实存在。
3. 必须是 Markdown；只能进 `资料/笔记/`；要有标题和几句介绍；动不了书。
4. 创建或更新必须针对**同一份稿**已经走过预览和求证。同一份稿 = 标题 + 介绍 + 正文；求证字段不算改稿。改了正文必须两步重走。

抬头里要能标：`generated`（Agent 写的）、`verify`、`sources`。

**Agent：** 写什么；看过类似条目之后，决定改旧的还是新开；去读依据、判断真假、填写求证结论。真假由 Agent 认。工具不联网查、不打分、不改书。怎么求证写在工具说明，仓库另附一份给 Agent 产品去挂的说明（`docs/写笔记求证.md`）；说明不能代替闸门。  
**输出：** 记下了，或拒绝原因。

### 4.5 净化（笔记、地图、整本退书）

**输入：** 「整理笔记」或「把这本坏书拿掉」。  
**代码：** 先扫 `资料/笔记/`、各书 `地图.md`、以及各书是否机械未入完（无文字层、超长块、同名章撞车、空心目录名、文件名乱码、缺地图），**只出清单不删**。Agent 提交后：笔记可删/改/合并；地图可改/标过期；整本可 `withdraw`（身份只能是 `书/<slug>`）。碰到某一章/导读/正文、或用 `delete` 去删书 → 整单拒绝。退书只去掉 `资料/书/<slug>/`，归档源文件不动；删除后索引失效。  
**Agent：** 笔记留谁、合并怎么写；哪一本未入完要不要整本退出。不改正文。  
**新替旧发生在这里**（以及写笔记时改旧页），不发生在书的某一章上。

借鉴卡帕西 LLM Wiki：原料（归档/书正文）不改；**编译的是地图不是书**；查询先看路标（含地图通道）再打开原文；lint 扫笔记、地图死链和书的未入完。笔记会进化。查询仍打在书+笔记+地图全体，读正文只读书和笔记。

### 4.6 自检（检修通道）

**输入：** 「最近有没有可优化的」「我想加/减某个能力」。  
**代码：** 交出最近的调用和失败记录（默认大约一周）；不改库、不改闸门、不改程序。  
**Agent：** 读记录，问你问题，给建议；若要改功能，按下面修理规范写提案。

**修理规范（焊死）：**

1. 先写清：要加还是要减、动哪扇门、对「书不改 / 先抬头后正文」有没有风险。  
2. 你点头。  
3. 先改说明书（本文和 `docs/功能流程.md`），再考虑改程序。  

第一版**不准** Agent 直接补丁 MCP 源代码。自检不是后台根据数据自己改自己。

---

## 5. 卡壳交还

任何一门某一步做不下去：

- 停，不要在工具里连着重试。  
- 带出：卡在哪一步、为什么、已经做成了什么、正门上还能怎么试。  
- Agent 拿着说明改输入、换策略、或问你。  
- 禁止：检索失败就灌全书；入库失败就编一本假书；净化失败就直接删文件。  
- 工具自己崩了：也当失败交出去，并说明是工具坏了，需要修。

同一错误要再试，由 Agent 换了输入再调，这样才修得及时。

---

## 6. 典型用法（验收时就按这些走）

1. 你把三本书丢进 `原始资料/`，让 Agent 收件箱入库 → `资料/书/` 出现三套带导读、**地图**和各块的 Markdown，源文件在归档。缺地图、或地图几乎不覆盖章名的那本不算入完。  
2. 聊天选出 B、C，Agent 自己找到文件后指定入库 → 只多这两本，收件箱里其它文件不动。  
3. Agent 找不到书 → 告诉你，不调用入库。  
4. 你让它用这个库谈拖延 → 先给**三栏路标**（两路都中 / 仅字面 / 仅语义）；它要再读某一章 → 只拿到那一块。交叉问题可以同时有两本书。  
5. 库里确实没有的拉丁专名 → 如实空。生活题（怕被讨厌、拖延）默认答案在库里，两路都弱也要给最像的结构命中并标明，不许装没有。  
6. 你让记下一条感悟 → 先看到类似笔记，求证标 `非事实`，再写入 `资料/笔记/`。读书笔记必须先读依据再标 `相符` 或 `部分不符`。  
7. 你让整理笔记 → 先清单后动手；笔记可改可删，书的某一章原封不动。  
8. 你问有没有可优化的 → 它根据记录问你、给建议，不私自改工具。  
9. 一件要跨好几篇才说得清的事（比如「矛盾论、实践论讲的道理，跟中国社会各阶级的分析有什么共同点」）→ 先给路标和命中分布；Agent 读一块、判断还要不要继续，不够就再检索、再点名。**工具不会因为它读得多就给奖励，也不会在它读够之前喊停。**  
10. 一个词命中 500 处、答案只在一篇里 → 工具如实报分布，Agent 自己克制。  
11. 「这本书大概讲什么」→ 读导读或地图一块，不许通读。  
12. 「总怕被讨厌怎么办」→ 地图应能把《被讨厌的勇气》送进路标，再点名读原文。  
13. 你让把一本已经入库的坏书拿掉 → Agent 先净化 scan，再 apply `withdraw` 点名 `书/<slug>`。书目录消失，检索不再有它；归档里的源文件还在，以后可以再入。不能退某一章。

逐条怎么判，见 `docs/验收标准.md`（Q1–Q14、W1–W20）。线上每轮机械跑 **Q1–Q14 + W1–W16**，按第九节打 **准 / 全 / 省**，明细追加 `检修/验收记录.md`，流水追加 `检修/打分记录.md`。W17–W20 只在 pytest 合成库。改了检索/入库/路标，没有打分新节不算做完。

没被问到来源，不要表演「根据《某某》第 n 章」。没读过就声称根据某章，写入会被拒绝。

---

## 7. 实现原则（轻量）

- 正本是文件。索引**必须有，但是派生物不是正本**：允许落在本机缓存（字面 FTS + 章向量），删了从 `资料/` 重建。禁止把索引或向量库当正本。  
- 字面索引用标准库 SQLite FTS5，**按列加权**（书名/章名/别名/地图/正文）。中文索引切二字片+单字+ASCII；查询片内相邻、禁止整句碎片 OR、片间不必全 AND。不上 jieba。**不要**把「关系」「分析」「变化」「发生」当停用词。  
- 语义召回用**章级向量**（一块一向量），只参与召回和排序，不进路标正文。模型可本地跑；不自研嵌入。两路融合用排序算术（如 RRF），候选池大于 5，输出最多 5 条路标。  
- 进口转换用现成工具（本机已有 markitdown），不要自研 PDF 解析。  
- 工具要少。六扇门不要拆成十几二十个平级工具。  
- 模型乱叫参数时尽量兼容常见别名（query/q、limit、target/targets 等）。阅读多块用字符串列表，不要对象数组。  
- 撞不上就返回空，宁可空也不硬凑。不上 RAG 灌 chunk。孤立单字语片才能命中单字帖。功能字若切开后两侧都是单字则不切（「目的论」）。

---

## 8. 环境与资产

| 项 | 说明 |
|---|---|
| 系统 | Windows，PowerShell 5.1，Python 3.11.4 |
| 本仓库 | `F:\project\knowledge\` |
| 可能接入的 Agent | 凡支持 MCP 的均可。`F:\nanobot\` 只是其中之一，不是本产品 |
| 待处理的书 | `F:\BaiduSyncdisk\obsidian\93_书籍馆\原始格式\待处理\` |
| markitdown | 环境变量 `KNOWLEDGE_MARKITDOWN` 指定的 convert.py；未设则找 `~/.claude/skills/markitdown/scripts/convert.py` |
| `F:\rag-system\` | 已有 RAG 服务。**不是本库形态**；仅当将来「找路标」加速才可能接，接口不变 |
| Obsidian 库 | `F:\BaiduSyncdisk\obsidian\`。本知识库不挂进去 |
| 开源对照 | 见 `参考笔记.md`。底座不直接用 basic-memory（AGPL、Python 3.12）或 OKF（短概念记忆），只借逻辑 |

---

## 9. 第一版明确不做

- 向量库 / RAG 当正本、按学科分多个向量库。章向量只做召回，路标仍只回身份  
- 把索引或向量缓存当成正本（缓存可扔，正本永远是 `资料/` 的 Markdown）  
- 用模型编的摘要**替换**书名/章名；地图可以生成，但不能当原文、不能当唯一检索面  
- 会话内隐藏状态（「翻页游标」之类）：续读靠 Agent 照邻块名字再点名，不靠工具记进度  
- 搜索结果带正文摘录；按查询自动把命中章灌进上下文  
- 把书编译成 wiki 之后不再提供原文  
- 定时后台入库、后台净化、后台自改代码  
- 网页形态（以后可加 `资料/网页/`，头和笔记一样）  
- 给人用的搜索页、挂进 Obsidian  
- Agent 无确认地修改本工具源码  
- 为一次读多块或求证新开平级工具；把六扇门拆成一堆平级工具（退书挂净化，不另开一门）  
- 后台自动退书、按章删书、删归档源文件、OCR  

---

## 10. 相关文档

| 文件 | 干什么 |
|---|---|
| 本文 `需求.md` | 现行规格 |
| `docs/版本.md` | 第几版、怎么编号；与 `mcp/pyproject.toml` 同步 |
| `docs/Skill-资料室.md` | 给人点名启用的用法；默认不暴露工具 |
| `docs/下一轮开工.md` | 开新对话贴进去的提示词 |
| `AGENTS.md` | 以后写代码的 Agent 先读这个 |
| `docs/功能流程.md` | 每一步谁干（人/Agent/代码），给人看 |
| `docs/计划.md` | 实现顺序与验收 |
| `参考笔记.md` | 开源对照；改检索/写回/净化前看映射表 |
| `README.md` | 给人看的入口 |
| `docs/写笔记求证.md` | 写笔记时 Agent 怎么求证（说明，不能代替闸门） |
| `docs/验收标准.md` | **冻结的验收标准**：题集 Q1–Q14、取词回捞 W1–W20、通过线、改造前基线、**第九节三维打分（准/全/省）** |
| `docs/问题.md` | 还没做的类问题（选题单，不是规格） |
| `docs/交接-检索准度问题.md` | **给接手 Agent 的检索准度问题交接单**：五类毛病 + 四条候选修法 + 基线 + 复现命令 |
| `docs/重构架构.md` | 检索层重构的架构（每块对应本文哪条需求） |
| `docs/借鉴与开源.md` | 直接用的库/模型 vs 只借鉴思路 vs 明确不用 |
| `docs/spike/` | **Spike（探路）**：调研/只读实验；不改代码 |
| `docs/本轮Spec.md` | **有这份才能改 `mcp/`**。重构落地前必须另写本轮 Spec |
| `docs/归档/2026-09-15-本轮Spec-仅字面最长原片.md` | 本轮已落地：仅字面最长原片 + 地图不用单字残片（计划 `docs/归档/2026-09-15-本轮计划-仅字面最长原片.md`；探路 `docs/归档/2026-09-15-spike-公共词撑满仅字面.md`） |
| `docs/归档/2026-09-15-本轮Spec-闲书建议块与节.md` | 上一轮已落地：闲书不出路标 + 建议块贴篇名 + 块内能点节（计划 `docs/归档/2026-09-15-noise-suggest-sections.md`） |
| `docs/归档/2026-09-14-本轮Spec-地图两路lint.md` | 上一轮已落地：地图 + 三栏 + lint（计划 `docs/归档/2026-09-14-map-two-path-lint.md`） |
| `docs/归档/2026-09-13-本轮Spec-自然篇与语片FTS.md` | 再上一轮已落地：自然篇 + 语片 FTS + 显式窗口（计划 `docs/归档/2026-09-13-natural-chunk-fts.md`） |  
| `docs/归档/2026-09-14-调研-三项目与检索层演进.md` | 调研：QMD/LightRAG/RAPTOR 与检索层演进；要不要上 RAG 前先看 |  
| `检修/验收记录.md` | 线上题**逐题明细**：占用、耗时、准/全/省、书本数/块数 |
| `检修/打分记录.md` | 三维打分**流水账**（只追加）。打开往下读就是每一轮；git 只防丢失 |
| `检修/提案/` | 某轮工作单；不覆盖本文 |
| `docs/归档/` | 旧讨论稿，不再生效 |

---

## 修订

| 日期 | 改了什么 |
|---|---|
| 2026-09-17 | **第三版检索落地**（`3.0.0`）：字段加权 FTS ∥ 假嵌入章向量 + RRF；栏名仅语义；第 10 轮准 23/29、全 31/32、省 26/26。规格归档 `docs/归档/2026-09-17-本轮Spec-第三版检索.md`。真嵌入模型未接。 |
| 2026-09-17 | **Skill 开门**：默认不暴露六扇门；人点名本库 Skill 后才接 MCP。闸门仍是代码。说明书 `docs/Skill-资料室.md`。客户端怎么挂 MCP 不在本轮 `mcp/` 实现里。 |
| 2026-09-17 | **检索层重构收口**：字面（字段加权 FTS）与章级向量并行，融合后最多 5 条路标；候选池大于 5。地图须覆盖主要非导航章名，可含别名/主题词。索引允许落成可扔缓存，仍不是正本。跨多本允许同单出现。架构 `docs/重构架构.md`，开源对照 `docs/借鉴与开源.md`。未改 `mcp/` 前须另写 `docs/本轮Spec.md`。 |
| 2026-09-16 | **退书挂净化**：`withdraw` 只退整本 `书/<slug>`；scan 标机械未入完；无文字层拒收（不 OCR）；空心 slug 走 `book-{hash8}`；删除后索引失效。书正文仍不改、不按章删。 |
| 2026-09-16 | **有效证据片 + 结构 aboutness**：问句证据片 = parse +《》整串 + 片尾功能字 + DF=0 长片只剥两边；MATCH/闸/最长片共用。两路都中须地图真有长片。结果集里已有结构命中时，丢掉只在正文沾到的书。无正文扫描块不进字面；导航件块名不进字面。第 7 轮：准 22/31、全 31/32、省 26/26（Q 组准 13/13）。 |
| 2026-09-15 | **仅字面最长原片 + 地图不用单字**：仅字面入路标要命中问句 `len≥3` 原片里最长的那一档（并列同长全算，**DF=0 也算门槛**，不再要求「在库长片 ≥2」）；最长档在库里谁都拿不出时退回现成 `LIT_GAP`。地图侧同样只认长原片，`看/办/来` 这类 `len<3` 残片不当地图命中，`_why_bits` 不再报单字。两路都中不动；不改 `index.py` 的 MATCH 与 `parse_query`；不把「分析/关系」当停用词。第 6 轮：准 9/13、全 31/32、省 12/12（与上轮同分；Q4 闲书 2 本 → 1 本，准是二值判定故题数未动）。 |
| 2026-09-15 | **三维打分**：验收按准/全/省分项记，不合成总分。改检索/入库/路标后必须全组跑题；明细 `检修/验收记录.md`，总表 `检修/打分记录.md`（进 git）。没有打分新行不算做完。题集未改。 |
| 2026-09-15 | **文档节奏**：需求唯一现行规格；进行中 Spike 在 `docs/spike/`；做完归档；类问题在 `docs/问题.md`。改代码只对问题类，禁止单题/单书特例。 |
| 2026-09-15 | **语片口径写清 + 节可点名**：片内相邻、禁止整句碎片 OR、片间不必全 AND；仅字面弱命中不占名额；阅读可点节名/`@偏移`。不改死规矩 8（两路都跑）。题集仍冻结。 |
| 2026-09-13 | **地图 + 两路检索**：死规矩 7–9；入库须有地图；路标三栏重合由代码标；人点名用库则默认答案在库里（PyTorch 仍空）。借卡帕西 ingest/query/lint，不借用 wiki 代替原文。 |  
| 2026-09-13 | **方案 B 收口**：入库配合检索改为「自然篇为文件 + 显式 8000 字窗口」，取消预切 4K 文件。检索改为语片 AND（非整句 OR）；停用表不含「关系/分析」；零命中不回退抬头。命中分布只报块数。 |  
| 2026-09-13 | **加「新三条」死规矩**：入库必须配合检索（取词回捞 + 点名可读完）、结构不决定可搜性、迭代检索「给判断的料不给判断」。死规矩 2 措辞由「不打开正文」改为「只回身份和命中理由，永不回正文」。检索改为内存全文索引。向量/RAG 明确不做并写明复访条件。 |
| 2026-09-13 | 检索加深改为一次可点名多块（块数/字数有顶，部分失败）；路标写到建议块；中文二字片。写笔记增加求证步：预览 → 求证 → 写入；`相符`/`部分不符` 必须先成功读过依据。 |
| 2026-09-12 | 重写为现行用户规格。六扇门、书/笔记分治、代码焊步骤、卡壳交还、自检不改代码。旧稿归档。 |
